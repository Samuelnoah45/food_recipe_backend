table:
  name: Ingredients
  schema: public
object_relationships:
  - name: food
    using:
      foreign_key_constraint_on: food_id
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - amount
        - ingredient_name
        - unit
        - food_id
        - Ingredient_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - amount
        - ingredient_name
        - unit
        - food_id
        - Ingredient_id
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns:
        - amount
        - ingredient_name
        - unit
        - food_id
        - Ingredient_id
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - amount
        - ingredient_name
        - unit
        - food_id
        - Ingredient_id
      filter: {}
      check: null
